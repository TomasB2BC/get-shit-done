---
phase: 13-project-alias-resolver
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - commands/gsd/add-phase.md
  - commands/gsd/add-todo.md
  - commands/gsd/audit-milestone.md
  - commands/gsd/auto.md
  - commands/gsd/check-todos.md
  - commands/gsd/complete-milestone.md
  - commands/gsd/debug.md
  - commands/gsd/discuss-phase.md
  - commands/gsd/execute-phase.md
  - commands/gsd/help.md
  - commands/gsd/insert-phase.md
  - commands/gsd/list-phase-assumptions.md
  - commands/gsd/map-codebase.md
  - commands/gsd/new-milestone.md
  - commands/gsd/new-project.md
  - commands/gsd/pause-work.md
  - commands/gsd/plan-milestone-gaps.md
  - commands/gsd/plan-phase.md
  - commands/gsd/progress.md
  - commands/gsd/quick.md
  - commands/gsd/remove-phase.md
  - commands/gsd/research-phase.md
  - commands/gsd/resume-work.md
  - commands/gsd/set-profile.md
  - commands/gsd/settings.md
  - commands/gsd/update.md
  - commands/gsd/verify-work.md
  - get-shit-done/workflows/add-phase.md
  - get-shit-done/workflows/add-todo.md
  - get-shit-done/workflows/audit-milestone.md
  - get-shit-done/workflows/auto-dispatch.md
  - get-shit-done/workflows/check-todos.md
  - get-shit-done/workflows/complete-milestone.md
  - get-shit-done/workflows/diagnose-issues.md
  - get-shit-done/workflows/discovery-phase.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/insert-phase.md
  - get-shit-done/workflows/list-phase-assumptions.md
  - get-shit-done/workflows/map-codebase.md
  - get-shit-done/workflows/new-milestone.md
  - get-shit-done/workflows/pause-work.md
  - get-shit-done/workflows/plan-milestone-gaps.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/progress.md
  - get-shit-done/workflows/quick.md
  - get-shit-done/workflows/remove-phase.md
  - get-shit-done/workflows/research-phase.md
  - get-shit-done/workflows/resume-project.md
  - get-shit-done/workflows/set-profile.md
  - get-shit-done/workflows/settings.md
  - get-shit-done/workflows/verify-phase.md
  - get-shit-done/workflows/verify-work.md
autonomous: true

must_haves:
  truths:
    - "Every GSD command file's argument-hint includes [--project <alias>]"
    - "Every workflow file that reads .planning/ has a Step 0 Project Resolution block"
    - "When --project is used, the workflow changes to the resolved project directory before any .planning/ reads"
    - "When --project is omitted, behavior is unchanged (backwards compatible)"
    - "--project flag is stripped from $ARGUMENTS before downstream parsing"
  artifacts:
    - path: "commands/gsd/execute-phase.md"
      provides: "Representative command with --project in argument-hint"
      contains: "--project"
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Representative workflow with Step 0 project resolution"
      contains: "Project Resolution"
  key_links:
    - from: "get-shit-done/workflows/execute-phase.md"
      to: "get-shit-done/bin/gsd-tools.js"
      via: "node gsd-tools.js resolve-project"
      pattern: "resolve-project"
---

<objective>
Add --project flag support across ALL GSD command and workflow files.

Purpose: Enable users to target sub-projects by alias from any GSD command (e.g., `/gsd:execute-phase 3 --project venntel`).
Output: All 27 command files updated with --project in argument-hint, all applicable workflow files updated with Step 0 Project Resolution block.
</objective>

<execution_context>
@C:\Users\tomas\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\tomas\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-project-alias-resolver/13-CONTEXT.md
@.planning/phases/13-project-alias-resolver/13-RESEARCH.md
@.planning/phases/13-project-alias-resolver/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --project to argument-hint in all command files</name>
  <files>
    commands/gsd/add-phase.md
    commands/gsd/add-todo.md
    commands/gsd/audit-milestone.md
    commands/gsd/auto.md
    commands/gsd/check-todos.md
    commands/gsd/complete-milestone.md
    commands/gsd/debug.md
    commands/gsd/discuss-phase.md
    commands/gsd/execute-phase.md
    commands/gsd/help.md
    commands/gsd/insert-phase.md
    commands/gsd/list-phase-assumptions.md
    commands/gsd/map-codebase.md
    commands/gsd/new-milestone.md
    commands/gsd/new-project.md
    commands/gsd/pause-work.md
    commands/gsd/plan-milestone-gaps.md
    commands/gsd/plan-phase.md
    commands/gsd/progress.md
    commands/gsd/quick.md
    commands/gsd/remove-phase.md
    commands/gsd/research-phase.md
    commands/gsd/resume-work.md
    commands/gsd/set-profile.md
    commands/gsd/settings.md
    commands/gsd/update.md
    commands/gsd/verify-work.md
  </files>
  <action>
For each command file in `commands/gsd/*.md` (EXCLUDING register-project.md which already has it from Plan 13-01), update the `argument-hint` line in the frontmatter to include `[--project <alias>]`.

**Pattern:** Prepend `[--project <alias>]` before existing arguments.

Examples of what to change:

```yaml
# Before:
argument-hint: "<phase-number> [--gaps-only]"
# After:
argument-hint: "[--project <alias>] <phase-number> [--gaps-only]"

# Before:
argument-hint: "[phase] [--research] [--skip-research] [--gaps] [--skip-verify]"
# After:
argument-hint: "[--project <alias>] [phase] [--research] [--skip-research] [--gaps] [--skip-verify]"

# Before:
argument-hint: "[--max-phases N] [--single-phase] [--resume]"
# After:
argument-hint: "[--project <alias>] [--max-phases N] [--single-phase] [--resume]"
```

**For files WITHOUT an argument-hint field** (some command files may not have one): add `argument-hint: "[--project <alias>]"` after the `description:` field.

**Do NOT modify:** register-project.md (already has it), join-discord.md (not project-related -- skip if it exists).

Process all 27 files systematically. For each file:
1. Read the file
2. Find the `argument-hint:` line
3. Prepend `[--project <alias>] ` to the existing hint value
4. If no argument-hint exists, add one after description
5. Write the file

IMPORTANT: Do not modify anything else in these files. Only the argument-hint line changes.
  </action>
  <verify>
```bash
# Count command files with --project in argument-hint (should be 28 including register-project.md)
grep -l "\-\-project" commands/gsd/*.md | wc -l

# Spot-check a few
grep "argument-hint" commands/gsd/execute-phase.md
grep "argument-hint" commands/gsd/plan-phase.md
grep "argument-hint" commands/gsd/auto.md
grep "argument-hint" commands/gsd/settings.md
```
  </verify>
  <done>All 27 command files (plus register-project.md from Plan 01) have `[--project <alias>]` in their argument-hint. Total: 28 files with --project.</done>
</task>

<task type="auto">
  <name>Task 2: Insert Step 0 Project Resolution in all workflow files</name>
  <files>
    get-shit-done/workflows/add-phase.md
    get-shit-done/workflows/add-todo.md
    get-shit-done/workflows/audit-milestone.md
    get-shit-done/workflows/auto-dispatch.md
    get-shit-done/workflows/check-todos.md
    get-shit-done/workflows/complete-milestone.md
    get-shit-done/workflows/diagnose-issues.md
    get-shit-done/workflows/discovery-phase.md
    get-shit-done/workflows/discuss-phase.md
    get-shit-done/workflows/execute-phase.md
    get-shit-done/workflows/insert-phase.md
    get-shit-done/workflows/list-phase-assumptions.md
    get-shit-done/workflows/map-codebase.md
    get-shit-done/workflows/new-milestone.md
    get-shit-done/workflows/pause-work.md
    get-shit-done/workflows/plan-milestone-gaps.md
    get-shit-done/workflows/plan-phase.md
    get-shit-done/workflows/progress.md
    get-shit-done/workflows/quick.md
    get-shit-done/workflows/remove-phase.md
    get-shit-done/workflows/research-phase.md
    get-shit-done/workflows/resume-project.md
    get-shit-done/workflows/set-profile.md
    get-shit-done/workflows/settings.md
    get-shit-done/workflows/verify-phase.md
    get-shit-done/workflows/verify-work.md
  </files>
  <action>
For each workflow file that reads `.planning/` files, insert a "Step 0: Project Resolution" block BEFORE the first numbered step (usually "## 1.").

**EXCLUDE the following workflow files** (they don't need Step 0):
- `execute-plan.md` -- Called as a subagent with project already resolved by execute-phase
- `help.md` -- No .planning/ reads
- `update.md` -- GSD self-update, no project context needed
- `transition.md` -- Internal transition logic
- `new-project.md` -- Handled separately in Plan 13-03 (auto-registration logic)

**The standardized Step 0 block to insert:**

```markdown
## 0. Project Resolution

```bash
PROJECT_ALIAS=""
if echo "$ARGUMENTS" | grep -q '\-\-project'; then
  PROJECT_ALIAS=$(echo "$ARGUMENTS" | grep -oP '(?<=--project\s)\S+')
  ARGUMENTS=$(echo "$ARGUMENTS" | sed 's/--project[[:space:]]\+[[:graph:]]\+//' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
fi

if [ -n "$PROJECT_ALIAS" ]; then
  PROJECT_DIR=$(node C:\Users\tomas\.claude/get-shit-done/bin/gsd-tools.js resolve-project "$PROJECT_ALIAS" --raw)
  if [ -z "$PROJECT_DIR" ]; then
    echo "[X] ERROR: Project alias '$PROJECT_ALIAS' not found"
    node C:\Users\tomas\.claude/get-shit-done/bin/gsd-tools.js resolve-project "$PROJECT_ALIAS"
    # Stop execution
  fi
  PROJECT_ROOT=$(dirname "$PROJECT_DIR")
  cd "$PROJECT_ROOT"
  echo ">> Resolved --project $PROJECT_ALIAS -> $PROJECT_ROOT"
fi
```
```

**Insertion point:** Find the first line matching `## 1.` (or `## 1 ` or the first numbered heading) and insert Step 0 BEFORE it.

**IMPORTANT notes:**
- The Step 0 block must appear INSIDE the `<process>` section (after `<process>` tag, before `## 1.`)
- Preserve all existing content -- do NOT modify any other steps
- Some workflows start with "## 1. Validate" or "## 1. Parse" -- insert Step 0 before whichever "## 1." exists
- If a workflow uses `<steps>` or different formatting, adapt placement but keep the same bash block

Process all 26 applicable workflow files systematically.
  </action>
  <verify>
```bash
# Count workflow files with "Project Resolution" (should be 26)
grep -rl "Project Resolution" get-shit-done/workflows/*.md | wc -l

# Spot-check key workflows
grep -A2 "## 0" get-shit-done/workflows/execute-phase.md
grep -A2 "## 0" get-shit-done/workflows/plan-phase.md
grep -A2 "## 0" get-shit-done/workflows/auto-dispatch.md

# Verify excluded files do NOT have Step 0
grep "Project Resolution" get-shit-done/workflows/execute-plan.md || echo "OK: execute-plan excluded"
grep "Project Resolution" get-shit-done/workflows/new-project.md || echo "OK: new-project excluded (handled in Plan 03)"
```
  </verify>
  <done>26 workflow files have Step 0 Project Resolution block inserted before their first numbered step. Excluded files (execute-plan.md, help.md, update.md, transition.md, new-project.md) do NOT have Step 0.</done>
</task>

</tasks>

<verification>
1. All 28 command files have `--project` in argument-hint (27 modified here + register-project.md from Plan 01)
2. 26 workflow files have "## 0. Project Resolution" block
3. Step 0 block uses `node gsd-tools.js resolve-project` for alias resolution
4. Step 0 strips `--project <alias>` from $ARGUMENTS before downstream parsing
5. Excluded files (execute-plan.md, help.md, update.md, transition.md, new-project.md) are NOT modified
6. No other content in any file is modified
</verification>

<success_criteria>
- Every GSD command shows --project in its argument-hint
- Every applicable workflow resolves --project before reading .planning/ files
- Backwards compatible: omitting --project keeps all behavior unchanged
- --project is cleanly stripped from $ARGUMENTS
- Spot-check: execute-phase.md, plan-phase.md, auto-dispatch.md all have correct Step 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-project-alias-resolver/13-02-SUMMARY.md`
</output>
