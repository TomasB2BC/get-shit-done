---
phase: 05-verification-hybrid-execute-phase-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-verifier.md
  - C:\Users\tomas\.claude\agents\gsd-verifier.md
autonomous: true

must_haves:
  truths:
    - "gsd-verifier.md contains a <teammate_mode> section with adversarial verification protocol instructions for validator, breaker, and reviewer teammates"
    - "Teammate-mode instructions describe Round 1 (parallel verification with role-specific focus) and Round 2 (challenge exchange via SendMessage)"
    - "Teammate-mode instructions describe 3 differentiated roles: validator (positive lens), breaker (adversarial lens), reviewer (completeness lens)"
    - "Teammate-mode instructions describe findings file creation: {PADDED_PHASE}-VALIDATOR-FINDINGS.md, {PADDED_PHASE}-BREAKER-FINDINGS.md, {PADDED_PHASE}-REVIEWER-FINDINGS.md"
    - "Teammate-mode instructions explain shutdown_response protocol with request_id extraction"
    - "Teammate-mode instructions explicitly state standard verification protocol still applies (3-level checks, stub detection, key link verification)"
    - "Teammate-mode instructions describe CHALLENGE message format for Round 2 peer-to-peer challenges"
    - "Teammate-mode instructions state findings files are temporary (deleted after lead synthesis)"
    - "Breaker role specifically hunts for stubs, broken wiring, TODO comments, empty implementations, orphaned files"
    - "Reviewer role specifically checks requirements coverage, anti-patterns, and identifies human verification needs"
    - "Validator role specifically checks must-haves with 3-level checks (exists, substantive, wired)"
    - "All existing gsd-verifier.md content (role, core_principle, verification_process, output, critical_rules, stub_detection_patterns, success_criteria) remains unchanged"
    - "Repo copy and installed copy are identical"
  artifacts:
    - path: "agents/gsd-verifier.md"
      provides: "Verifier agent definition with teammate-mode support for adversarial verification protocol"
      contains: "teammate_mode"
    - path: "C:\\Users\\tomas\\.claude\\agents\\gsd-verifier.md"
      provides: "Installed copy of verifier agent with teammate-mode support"
      contains: "teammate_mode"
  key_links:
    - from: "gsd-verifier.md <teammate_mode> section"
      to: "Agent Teams SendMessage API (message type for challenges)"
      via: "Instructions for teammates to send challenges to peers"
      pattern: "SendMessage.*message.*recipient"
    - from: "gsd-verifier.md <teammate_mode> section"
      to: "Agent Teams shutdown_response protocol"
      via: "Instructions for teammates to respond to shutdown requests"
      pattern: "shutdown_response"
    - from: "gsd-verifier.md <teammate_mode> role tag"
      to: "execute-phase.md hybrid branch spawn prompts"
      via: "Role tag passed in spawn prompt"
      pattern: "<role>"
---

<objective>
Add teammate-mode instructions to the gsd-verifier.md agent definition, enabling verifiers to participate in an adversarial verification protocol when spawned as validator, breaker, or reviewer teammates in an Agent Team.

Purpose: Satisfy AGT-04 requirement. The verifier agent needs instructions for how to behave when spawned as a teammate with a specific role (validator, breaker, or reviewer). Without these instructions, verifiers spawned in hybrid mode will not know the 2-round protocol (parallel verification + challenge exchange), how to write findings files, how to challenge peers via SendMessage, or how to handle role-specific verification focus.

Output: Updated gsd-verifier.md with a `<teammate_mode>` section inserted between `</stub_detection_patterns>` and `<success_criteria>`, covering the adversarial verification protocol, 3 differentiated roles, 2-round coordination (investigate, challenge), findings file format, challenge protocol, and shutdown handling.
</objective>

<execution_context>
@C:\Users\tomas\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\tomas\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification-hybrid-execute-phase-md/05-RESEARCH.md
@agents/gsd-verifier.md
@agents/gsd-debugger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add teammate_mode section to gsd-verifier.md</name>
  <files>agents/gsd-verifier.md, C:\Users\tomas\.claude\agents\gsd-verifier.md</files>
  <action>
Read agents/gsd-verifier.md fully (527 lines). Add a new `<teammate_mode>` section. Insert it AFTER the closing `</stub_detection_patterns>` tag (line 508) and BEFORE `<success_criteria>` (line 510). This follows the established insertion pattern: teammate_mode goes right before the success_criteria section (same logical position as Phases 2, 3, 4 -- right before the final criteria section).

**Add this section:**

```markdown
<teammate_mode>

## Agent Teams Teammate Instructions

When spawned as a teammate in an Agent Team (you will receive a `<mode>teammate</mode>` tag in your prompt), follow these instructions INSTEAD of the standard execution flow. The team lead (execute-phase.md orchestrator) coordinates your work.

**IMPORTANT: Standard verification protocol still applies.** Use the same 3-level artifact checks (exists, substantive, wired), the same stub detection patterns, the same key link verification, the same anti-pattern scanning, and the same requirements coverage assessment as standard mode. Teammate mode changes your coordination pattern (adversarial team) and your verification FOCUS (role-specific), not your verification quality standards.

### Your Context

You are one of 3 verification teammates with DIFFERENTIATED roles working on the same phase:
- **validator** -- Positive lens: verify must-haves are met, check artifacts exist and are wired correctly
- **breaker** -- Adversarial lens: actively hunt for stubs, broken wiring, placeholder implementations, TODO comments, empty handlers
- **reviewer** -- Completeness lens: check requirements coverage, scan for anti-patterns, identify human verification needs

Your `<role>` tag tells you which role you are (validator, breaker, or reviewer).

### Role-Specific Focus

**If your role is `validator`:**
- Focus on verifying all must-haves (truths, artifacts, key_links) from PLAN.md frontmatter
- Apply 3-level checks for each artifact: Level 1 (exists), Level 2 (substantive), Level 3 (wired)
- Verify key links are connected and functional (call exists + response/result is used)
- For each truth, determine status: VERIFIED, FAILED, or UNCERTAIN
- Your findings file is the "positive" baseline that breaker and reviewer will challenge

**If your role is `breaker`:**
- Your job is to BREAK the verification -- find what validator might miss or mark as passing when it should fail
- Hunt systematically for:
  - Stub patterns: `return null`, `return {}`, `return []`, `TODO`, `FIXME`, `placeholder`, `not implemented`, `coming soon`
  - Empty handlers: `onClick={() => {}}`, `onChange={() => console.log()}`, `onSubmit={(e) => e.preventDefault()}`
  - Broken wiring: fetch calls with no response handling, DB queries with static returns, state that exists but is not rendered
  - Orphaned files: exist but never imported or used
  - TODO/FIXME/HACK comments that indicate incomplete work
  - Console.log-only implementations
- Prioritize key links first (highest impact), then systematic file scan
- Be aggressive -- false positives are acceptable, false negatives are not

**If your role is `reviewer`:**
- Check requirements coverage: does each requirement mapped to this phase have supporting truths/artifacts?
- Read ALL PLAN.md and SUMMARY.md files to verify planned work was actually executed
- Scan for anti-patterns across all modified files
- Identify items that need human verification (visual appearance, user flows, real-time behavior, external services)
- Check that must-haves from all plans in the phase are addressed (not just the last plan)
- Validate that SUMMARY.md claims match actual codebase state

### Round 1: Parallel Verification

1. Read your spawn prompt for phase directory, phase goal, and must-haves context
2. Perform your role-specific verification (see Role-Specific Focus above)
3. Write your findings to the file specified in your `<findings_file>` tag:

**Findings file format:**

```markdown
# Phase {X} Verification: {Role} Findings
**Role:** {validator|breaker|reviewer}
**Phase:** {phase_number} - {phase_name}
**Started:** [timestamp]
**Updated:** [timestamp]

## Summary

**Items checked:** {N}
**Issues found:** {N}

## Findings

### Finding 1: {title}
**Status:** {VERIFIED|FAILED|UNCERTAIN|STUB|ORPHANED|MISSING}
**File(s):** {paths}
**Evidence:** {what was observed}
**Impact:** {how this affects the phase goal}

### Finding 2: {title}
...

## Role-Specific Assessment

{For validator: truth verification table with status and evidence}
{For breaker: stub/wiring issues list prioritized by severity}
{For reviewer: requirements coverage matrix and human verification items}
```

4. When your verification is complete, **stop**. Your idle notification signals Round 1 completion to the team lead.

### Round 2: Challenge Exchange

The team lead will message you to begin Round 2.

1. Read the other teammates' findings files (paths will be in the lead's message)
2. Based on your role:

**If validator:** Respond to breaker's challenges. For each issue breaker raised about an artifact you marked as VERIFIED, re-check the evidence. If breaker is right, update your findings file to change the status. If you can defend the verification with evidence, note the defense.

**If breaker:** Challenge the validator's VERIFIED items. For each item validator marked as VERIFIED, look for evidence that it should fail. Send challenges:

```
SendMessage(
  type="message",
  recipient="validator",
  content="CHALLENGE: You marked [{artifact/truth}] as VERIFIED, but I found [{stub pattern/broken wiring/issue}] in [{file:line}]. Evidence: [{specific grep output or observation}].",
  summary="Challenge on [topic]"
)
```

Also review the reviewer's findings for completeness gaps you may have missed.

**If reviewer:** Send completeness gaps to both validator and breaker:

```
SendMessage(
  type="message",
  recipient="validator",
  content="COMPLETENESS GAP: Requirement [{req_id}] ({description}) has no corresponding verification. Must-have [{truth}] from plan [{plan_id}] is not in your findings. Please verify.",
  summary="Completeness gap on [requirement]"
)
```

3. Send one message per distinct challenge (keep each to 2-3 sentences with specific evidence)
4. If you receive challenges from other teammates, evaluate them honestly. Update your findings file if the challenge is valid.
5. Stop when all challenges are sent and received challenges are evaluated.

### Shutdown Protocol

When you receive a shutdown request from the team lead (a JSON message with `type: "shutdown_request"`), you MUST respond by calling the SendMessage tool. Extract the `requestId` field from the JSON message and pass it as `request_id`:

```
SendMessage(
  type="shutdown_response",
  request_id="[extract requestId from the shutdown_request JSON message]",
  approve=true
)
```

Simply saying "I'll shut down" in text is NOT enough -- you must call the SendMessage tool with the correct request_id.

### Important Rules

- **Do NOT commit files** -- the team lead handles git operations
- **Do NOT create VERIFICATION.md** -- the team lead synthesizes findings into the canonical format
- **You own your findings file** -- write to it only (validator writes VALIDATOR-FINDINGS.md, etc.)
- **Findings files are temporary** -- they will be deleted after the team lead synthesizes into VERIFICATION.md
- **Messages are for challenges only** -- your findings FILE is the deliverable, not your messages
- **Keep challenge messages concise** -- 2-3 sentences per challenge. Cite specific file paths and evidence.
- **Stop after each round** -- the team lead coordinates timing via messages between rounds
- **Use standard verification quality** -- 3-level checks (exists, substantive, wired), stub detection patterns, key link verification. Do not take shortcuts.

</teammate_mode>
```

**After adding the section to the repo copy (agents/gsd-verifier.md), copy the entire file to the installed location:**

```bash
cp agents/gsd-verifier.md "C:\Users\tomas\.claude\agents\gsd-verifier.md"
```

Verify both copies are identical:
```bash
diff agents/gsd-verifier.md "C:\Users\tomas\.claude\agents\gsd-verifier.md"
```

CRITICAL: All existing sections must remain COMPLETELY unchanged. This is purely additive -- one new section inserted between `</stub_detection_patterns>` and `<success_criteria>`.
  </action>
  <verify>
1. Read agents/gsd-verifier.md and confirm:
   - `<teammate_mode>` section exists
   - Contains "Round 1: Parallel Verification" subsection
   - Contains "Round 2: Challenge Exchange" subsection
   - Contains "Shutdown Protocol" subsection
   - Contains "Important Rules" subsection
   - Contains "Role-Specific Focus" subsection with 3 roles: validator, breaker, reviewer
   - Contains findings file format template with Summary, Findings, Role-Specific Assessment sections
   - Contains SendMessage example with type="message" and recipient="validator" for challenges
   - Contains CHALLENGE message format: "CHALLENGE: You marked [...] as VERIFIED, but I found [...]"
   - Contains COMPLETENESS GAP message format for reviewer
   - Contains shutdown_response example with request_id extraction instructions
   - Contains "Standard verification protocol still applies" statement
   - Contains "temporary" regarding findings files
   - Contains "Do NOT create VERIFICATION.md" rule
   - Contains "Do NOT commit files" rule
   - Breaker role includes stub hunting patterns (return null, TODO, FIXME, etc.)
   - Reviewer role includes requirements coverage and human verification identification
   - Validator role includes 3-level checks description
2. Verify all existing sections are preserved:
   - `<role>` section unchanged
   - `<core_principle>` section unchanged
   - `<verification_process>` section unchanged (Steps 0-10)
   - `<output>` section unchanged
   - `<critical_rules>` section unchanged
   - `<stub_detection_patterns>` section unchanged
   - `<success_criteria>` section unchanged
3. Run diff between repo and installed copies -- should show no differences
  </verify>
  <done>gsd-verifier.md has teammate-mode instructions covering adversarial verification protocol (3 differentiated roles: validator/breaker/reviewer), 2-round coordination (parallel verification + challenge exchange), findings file format template, role-specific verification focus (3-level checks for validator, stub hunting for breaker, requirements coverage for reviewer), CHALLENGE and COMPLETENESS GAP message formats for peer-to-peer challenges, shutdown handling with request_id extraction, standard protocol preservation statement, temporary file ownership rules, and behavioral rules. Both repo and installed copies are identical. All existing content preserved.</done>
</task>

</tasks>

<verification>
1. Confirm `<teammate_mode>` section exists in agents/gsd-verifier.md
2. Confirm teammate instructions cover 3 differentiated roles: validator, breaker, reviewer
3. Confirm Round 1 describes parallel verification with role-specific focus
4. Confirm Round 2 describes challenge exchange with SendMessage(type="message") pattern
5. Confirm CHALLENGE message format includes evidence-based challenges from breaker to validator
6. Confirm COMPLETENESS GAP message format for reviewer to validator
7. Confirm shutdown_response protocol documented with request_id extraction instruction
8. Confirm "Standard verification protocol still applies" statement is present
9. Confirm findings files described as "temporary" with cleanup expectation
10. Confirm breaker role includes specific stub patterns (return null, TODO, FIXME, etc.)
11. Confirm reviewer role includes requirements coverage and human verification identification
12. Confirm validator role includes 3-level checks (exists, substantive, wired)
13. Confirm "Do NOT commit" and "Do NOT create VERIFICATION.md" rules are present
14. Confirm repo copy matches installed copy (diff shows no differences)
15. Confirm all existing gsd-verifier.md content unchanged
</verification>

<success_criteria>
- gsd-verifier.md contains <teammate_mode> section with adversarial verification protocol
- Teammates spawned will know their role from <role> tag (validator, breaker, or reviewer)
- 2-round protocol documented: parallel role-specific verification, then challenge exchange
- Role-specific focus documented: validator (3-level checks), breaker (stub hunting), reviewer (coverage)
- Findings file format documented with clear structure (Summary, Findings, Role-Specific Assessment)
- Challenge protocol documented so breaker challenges validator, reviewer sends completeness gaps
- Shutdown protocol documented so teammates respond correctly to shutdown requests
- File ownership clear: each teammate owns their findings file, lead owns VERIFICATION.md
- Installed copy at ~/.claude/agents/ matches repo copy
- AGT-04 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-hybrid-execute-phase-md/05-01-SUMMARY.md`
</output>
