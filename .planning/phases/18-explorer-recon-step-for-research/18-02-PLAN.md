---
phase: 18-explorer-recon-step-for-research
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-phase-researcher.md
autonomous: true

must_haves:
  truths:
    - "gsd-phase-researcher.md contains a recon_context section instructing the researcher to use the injected recon block to sharpen research angle and skip first-pass discovery"
    - "The recon_context section documents the PROBLEM, EXISTING, CONSTRAINTS, NOT-ABOUT, and TEAM-HINT fields that will be injected"
    - "The researcher is instructed to skip discovery work that the NOT-ABOUT line explicitly marks as already covered by recon"
    - "The recon_context section applies to both standard mode and teammate mode (all roles benefit from recon injection)"
    - "The section is additive only -- no existing sections are removed or modified"
  artifacts:
    - path: "agents/gsd-phase-researcher.md"
      provides: "Researcher agent with recon_context awareness"
      contains: "recon_context"
  key_links:
    - from: "agents/gsd-phase-researcher.md"
      to: "get-shit-done/workflows/plan-phase.md"
      via: "researcher reads recon_context block injected by workflow Stage 4.5"
      pattern: "recon_context"
    - from: "agents/gsd-phase-researcher.md"
      to: "get-shit-done/workflows/research-phase.md"
      via: "researcher reads recon_context block injected by workflow Step 3.5"
      pattern: "recon_context"
---

<objective>
Add a `<recon_context>` section to gsd-phase-researcher.md that instructs the researcher agent how to use the recon injection block when present in its prompt.

Purpose: When the explorer recon step runs before research, the orchestrator injects a `<recon_context>` block into researcher prompts. The researcher needs explicit instructions on how to use this block: sharpen research angle, skip already-covered discovery, and respect the NOT-ABOUT constraint. Without this section, researchers may ignore the injected block or redundantly rediscover what recon already found.

Output: Modified gsd-phase-researcher.md with a new `<recon_context>` section.
</objective>

<execution_context>
@C:\Users\tomas\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\tomas\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-explorer-recon-step-for-research/18-RESEARCH.md
@agents/gsd-phase-researcher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recon_context section to gsd-phase-researcher.md</name>
  <files>agents/gsd-phase-researcher.md</files>
  <action>
Add a new `<recon_context>` section to `agents/gsd-phase-researcher.md`. Insert it AFTER the `<upstream_input>` section and BEFORE the `<downstream_consumer>` section. This placement puts it right after the section about CONTEXT.md (user input) and before the section about RESEARCH.md output format -- logical since recon is another input source.

The section content:

```xml
<recon_context>

## Using Recon Context (When Present)

When spawned with a `<recon_context>` block in your prompt, field recon explorers have already scanned the codebase and provided structured orientation. Use this to sharpen your research -- do NOT ignore it or re-discover what it already covers.

**The recon_context block contains:**

| Field | What It Tells You | How to Use It |
|-------|-------------------|---------------|
| `PROBLEM:` | Dense summary of the core technical problem | Start your research from this framing, not from scratch |
| `EXISTING:` | Key existing code/patterns directly relevant | Reference these in your findings; do not present them as "discoveries" |
| `CONSTRAINTS:` | Non-obvious limits the researcher must respect | Verify your recommendations respect these constraints |
| `NOT-ABOUT:` | What the recon explorers already covered | **Skip this discovery work entirely** -- do not spend tokens rediscovering it |
| `TEAM-HINT:` | Why this team composition was chosen | Understand your role in context of the overall research approach |

**Rules when recon_context is present:**

1. **Read NOT-ABOUT first.** This tells you what ground is already covered. Do not waste research effort rediscovering it.
2. **Use PROBLEM as your starting frame.** The explorers have already oriented to the problem space. Begin your deep research from their framing, not from a blank slate.
3. **Reference EXISTING patterns.** When recommending architecture or patterns, acknowledge what already exists in the codebase and build on it rather than proposing alternatives to established conventions.
4. **Verify against CONSTRAINTS.** Before finalizing any recommendation, check it does not violate the constraints the explorers identified.
5. **Adapt to TEAM-HINT.** If the team composition is "collaborative" rather than "adversarial," adjust your tone accordingly. Domain-specialist teams focus on depth in a specific area; minimal teams need comprehensive solo coverage.

**When recon_context is NOT present:** Proceed with standard research protocol. The absence of recon context means recon was skipped (config off, CONTEXT.md exists, or recon already done). This is normal -- not an error.

**Applies to all roles:** In teammate mode, all roles (optimist, devil's advocate, explorer) benefit from recon context. The optimist uses it for framing; the devil's advocate uses CONSTRAINTS to find gaps; the explorer uses NOT-ABOUT to avoid retreading covered ground.

</recon_context>
```

**IMPORTANT constraints:**
- Do NOT modify any existing sections (role, upstream_input, downstream_consumer, philosophy, tool_strategy, etc.)
- Insert the new section between `</upstream_input>` and `<downstream_consumer>` -- nowhere else
- The section is purely additive -- it adds instructions for a new input source, similar to how upstream_input documents CONTEXT.md
- Keep the field names (PROBLEM, EXISTING, CONSTRAINTS, NOT-ABOUT, TEAM-HINT) consistent with the RECON.md template defined in the research document
  </action>
  <verify>
Verify the recon_context section exists and contains all required elements:
```bash
grep -c "recon_context" agents/gsd-phase-researcher.md
grep -c "NOT-ABOUT" agents/gsd-phase-researcher.md
grep -c "TEAM-HINT" agents/gsd-phase-researcher.md
grep -c "PROBLEM:" agents/gsd-phase-researcher.md
grep -c "EXISTING:" agents/gsd-phase-researcher.md
grep -c "CONSTRAINTS:" agents/gsd-phase-researcher.md
grep -c "all roles" agents/gsd-phase-researcher.md
```
All greps should return >= 1. Additionally verify section placement:
```bash
# recon_context should appear after upstream_input and before downstream_consumer
grep -n "upstream_input\|recon_context\|downstream_consumer" agents/gsd-phase-researcher.md
```
The recon_context line numbers should be between upstream_input and downstream_consumer line numbers.
  </verify>
  <done>
gsd-phase-researcher.md has a new recon_context section between upstream_input and downstream_consumer. The section documents all 5 recon injection fields (PROBLEM, EXISTING, CONSTRAINTS, NOT-ABOUT, TEAM-HINT) with usage instructions. It includes 5 rules for when recon context is present, handles the absent case (normal, not an error), and notes applicability to all teammate roles. No existing sections were modified.
  </done>
</task>

</tasks>

<verification>
The researcher agent file contains the recon_context section with proper placement and content:
```bash
# Section exists
grep "recon_context" agents/gsd-phase-researcher.md && echo "[OK] recon_context section present"

# All 5 fields documented
grep "PROBLEM:" agents/gsd-phase-researcher.md && echo "[OK] PROBLEM field"
grep "EXISTING:" agents/gsd-phase-researcher.md && echo "[OK] EXISTING field"
grep "CONSTRAINTS:" agents/gsd-phase-researcher.md && echo "[OK] CONSTRAINTS field"
grep "NOT-ABOUT:" agents/gsd-phase-researcher.md && echo "[OK] NOT-ABOUT field"
grep "TEAM-HINT:" agents/gsd-phase-researcher.md && echo "[OK] TEAM-HINT field"

# Placement between upstream_input and downstream_consumer
grep -n "upstream_input\|recon_context\|downstream_consumer" agents/gsd-phase-researcher.md

# Existing sections untouched
grep "upstream_input" agents/gsd-phase-researcher.md && echo "[OK] upstream_input preserved"
grep "downstream_consumer" agents/gsd-phase-researcher.md && echo "[OK] downstream_consumer preserved"
grep "teammate_mode" agents/gsd-phase-researcher.md && echo "[OK] teammate_mode preserved"
```
</verification>

<success_criteria>
1. gsd-phase-researcher.md has a recon_context section documenting how to use the recon injection block
2. All 5 fields (PROBLEM, EXISTING, CONSTRAINTS, NOT-ABOUT, TEAM-HINT) are documented with usage guidance
3. The NOT-ABOUT rule is prominent (Rule 1: read it first, skip that discovery)
4. The section applies to both standard and teammate modes
5. The section is placed between upstream_input and downstream_consumer
6. No existing sections were removed or modified
</success_criteria>

<output>
After completion, create `.planning/phases/18-explorer-recon-step-for-research/18-02-SUMMARY.md`
</output>
